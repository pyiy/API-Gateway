FROM node:18-alpine

WORKDIR /app

# 复制所有文件
COPY . .

# 安装依赖并构建
RUN npm install --no-audit --no-fund
RUN npm run build

# 设置环境变量
ENV NODE_ENV=production
ENV PORT=3000

EXPOSE 3000

CMD ["npm", "start"]
